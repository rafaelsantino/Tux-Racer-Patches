# Patches for Tux Racer

this repository contains various patches for tux racer (commercial edition)

# Patches Description:

enable tux racer editor.bps is a patch to enable the hidden course editor that never gets loaded, you must apply the patch specifically to squid_init.tcl also copy the files from the "required/enable tux racer editor" directory.
